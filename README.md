# WeatherBot---Dialogflow-Webhook-Service
This project is a Spring Boot application that acts as a webhook service for a Dialogflow ES chatbot. It fetches current weather and 5-day forecasts for a specified city using the OpenWeatherMap API and responds back to Dialogflow.
